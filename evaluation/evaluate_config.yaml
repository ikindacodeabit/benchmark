# SPDX-FileCopyrightText: Copyright (c) 1993-2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
# SPDX-License-Identifier: Apache-2.0

# This is the only YAML file passed to model-running commands. Local reference
# configurations under yml/ are intentionally excluded from Git.
#
# Default target: Qwen3.5-27B-GPTQ-Int4, synthetic-kv 64k, KVzip reference
# pass (compression_ratio=0.01). max_new_tokens/max_context_length are null
# (native/per-row defaults) and fraction is 1.0 (full dataset), matching
# yml/evaluate_synthetic_kv_64k_qwen35_27b_gptq_all_budgets.yaml -- same
# output_dir, so results/skip-if-exists are shared between the two.
output_dir: "../benchmark_artifacts/results/synthetic_kv/64k/runs/results_synthetic_kv_64k_qwen35_27b_gptq_all_budgets"

model: "/home/rethinkingai-self/25m0820/kvpress/Qwen3.5-27B-GPTQ-Int4"
dataset: "synthetic_kv"
data_dir:
  - "64k"

press_name: "kvzip"
compression_ratio: 0.01
memory_budget: null
memory_budget_unit: "MB"

fraction: 1.0
max_new_tokens: null
max_context_length: null
query_aware: false
needle_depth: null
synthetic_kv_metadata_override: false
seed: 42

device: "cuda:0"
fp8: false
int8: false
int4: false
model_kwargs: {}
